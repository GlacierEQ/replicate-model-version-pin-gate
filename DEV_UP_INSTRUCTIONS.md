# DEV_UP_INSTRUCTIONS — COMPLETED

The scaffold has been replaced by a real immutable model-version binding engine. Production rejects floating aliases; lower environments may resolve explicitly registered aliases to immutable versions. Behavioral/adversarial tests, an executable operate proof, packaging, and an installed CLI are included.

Proof:

```bash
python -m pytest -q
python scripts/operate.py
```

No Replicate affiliation or proprietary access is claimed.
